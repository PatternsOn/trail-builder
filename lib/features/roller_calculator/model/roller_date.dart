class RollerData {
  final int distance;
  final int height;

  RollerData({
    required this.distance,
    required this.height,
  });

  @override
  String toString() {
    return 'RollerData{distance: $distance, height: $height}';
  }
}
